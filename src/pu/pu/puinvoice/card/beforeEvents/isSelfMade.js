/*
 * @Author: jiangfw 
 * @PageInfo: 是否自制 
 * @Date: 2018-06-12 18:54:57 
 * @Last Modified by: jiangfw
 * @Last Modified time: 2018-07-27 10:24:49
 */
import { FIELD } from '../../constance';
export default function isSelfMade(props, areaIds) {
	// let bodyArea = areaIds.bodyArea;
	// let flag = true;
	// let rowNum = props.editTable.getNumberOfRows(bodyArea);
	// if (rowNum > 0) {
	// 	for (let row = 0; row < rowNum; row++) {
	// 		let csourcetypecode = props.editTable.getValByKeyAndIndex(bodyArea, row, FIELD.csourcetypecode);
	// 		if (csourcetypecode && null != csourcetypecode.value) {
	// 			flag = false;
	// 			break;
	// 		}
	// 	}
	// }
	// return flag;
}
